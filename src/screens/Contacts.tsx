import { Text, View } from 'react-native'

export function Contacts() {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        padding: 32,
      }}
    >
      <Text>Contacts</Text>
    </View>
  )
}
